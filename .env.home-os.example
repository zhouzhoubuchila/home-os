# Docker 容器能够访问的 Home Assistant 地址；不要填写长期令牌。
NAVET_HASS_URL=http://192.168.1.10:8123

# 公网 HTTPS 域名。部署前把 DNS 指向 Docker 主机，并转发 80/443。
HOME_OS_DOMAIN=home.example.com

# 可选：固定安装密钥。留空时容器首次启动会安全生成并保存在数据卷。
# 仅允许 64 位小写十六进制；不要提交真实值。
NAVET_INSTALLATION_KEY=
