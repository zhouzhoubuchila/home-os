import type { CalendarDevice, WeatherDevice } from '@navet/app/types/device.types';

export * from '@navet/core/provider-feature-models';

export type PlatformWeatherDevice = WeatherDevice;
export type PlatformCalendarDevice = CalendarDevice;
